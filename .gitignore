# MkDocs build output — never commit
site/

# Auto-generated publish directory — rebuilt by publish.sh on every deploy
published/

# Generated SVG files — produced alongside PNGs but not committed or published
diagrams/svg/

# Brochure PDF — generated on publish, not committed
tbs-brochure.pdf

# Claude Code session files
.claude/
